# Script Instructions

## Scope

This directory contains developer and operational helper scripts.

## Rules

- Scripts must not assume real secrets live in the repository.
- Scripts should accept config paths through arguments or environment variables.
- Avoid destructive actions by default.
- Print clear errors when required local config is missing.
